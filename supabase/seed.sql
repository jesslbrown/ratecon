-- Seed file for local development. Runs after migrations on `supabase db reset`.
-- Insert deterministic fixtures that don't depend on a real auth.users row;
-- create test users via the Supabase Studio or auth API instead.

-- Example: pre-populated equipment types could go here once we add a table
-- for them. For now this file is intentionally empty so resets are fast.
